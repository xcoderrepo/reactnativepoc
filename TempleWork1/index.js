/** @format */

import {AppRegistry} from 'react-native';
import {RouterNavigator} from './src/RouterNavigator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RouterNavigator);
